apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openIM.fullname" . }}-test-connection"
  labels:
    {{- include "openIM.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openIM.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
